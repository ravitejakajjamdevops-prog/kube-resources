apiVersion: v1  
kind: Service
metadata: 
  name: service-demo
spec:
  selector:
     project: roboshop
     component: frontend
     environment: dev

  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080