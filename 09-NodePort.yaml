apiVersion: v1
kind: Service
metadata:
  name: nginx-nodePort
spec:
  type: nodePort
  selector:
    project: roboshop
    component: frontend
    environment: dev
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    
