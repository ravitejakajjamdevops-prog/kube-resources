apiVersion: v1
kind: Service
metadata: 
 name: nginx-loadbal
spec:
  selector:
    project: roboshop
    component: frontend
    environment: dev
  ports:
  -  protocol: TCP
     port: 80
     targetPort: 80
     nodePort: 32525